pipeline {
    agent any  

        stages {
        stage('Deploy') { 
            steps {
                ansiblePlaybook inventory: 'deployment/inventory.yml', playbook: 'deployment/deploy-petclinic.yml', extras: "-e BUILD_ID=${BUILD_ID}"
            }
        }
    }
}